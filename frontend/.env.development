VITE_API_ROOT=http://localhost:8080
# Kết nối thời gian thực đi qua cùng cổng Kong như mọi yêu cầu khác — giống
# production (https://api.../realtime). Trước đây dev nối thẳng vào cổng 3001
# của realtime-gateway, nên một lỗi định tuyến WebSocket ở Kong chỉ lộ ra khi
# đã lên môi trường thật.
VITE_SOCKET_URL=http://localhost:8080/realtime
